# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1          # includes uvloop + httptools (matched to Dockerfile CMD)
websockets==13.1                   # FastAPI WebSocket transport
python-multipart==0.0.12           # required for form data (Twilio webhooks)

# ── Settings ───────────────────────────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.6.1           # BaseSettings with .env support

# ── Supabase ───────────────────────────────────────────────────────────────────
supabase==2.10.0                   # async client (supabase._async.client)
httpx==0.27.2                      # supabase-py peer dep (pin to avoid conflicts)

# ── Redis ──────────────────────────────────────────────────────────────────────
redis==5.2.0                       # redis.asyncio for async client

# ── Celery (task queue) ───────────────────────────────────────────────────────
celery==5.4.0
kombu==5.3.7                       # Celery messaging (peer dep — pin for stability)

# ── LLM ────────────────────────────────────────────────────────────────────────
groq==0.11.0                       # Official Groq SDK — AsyncGroq

# ── Azure Speech (STT + TTS) ──────────────────────────────────────────────────
azure-cognitiveservices-speech==1.41.1

# ── Twilio ─────────────────────────────────────────────────────────────────────
twilio==9.3.6                      # Twilio helper lib (webhook validation etc.)

# ── Utilities ──────────────────────────────────────────────────────────────────
python-dotenv==1.0.1               # .env loading (pydantic-settings uses this)